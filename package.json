{
  "name": "simple-web-push",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node ./generateVapidKeys.js && next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "kill": "pkill -f 'next dev' || true && pkill -f 'next start' || true && pkill -f 'node.*3001' || true && lsof -ti:3001 | xargs kill -9 2>/dev/null || true"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "^18",
    "react-dom": "^18",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/web-push": "^3.6.3",
    "prettier": "^3.3.3",
    "typescript": "^5"
  }
}
